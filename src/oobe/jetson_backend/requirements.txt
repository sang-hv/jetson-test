# DEPRECATED: Use /src/requirements.txt instead.
#
# Kept for backward compatibility with older deploy scripts/tools.

# Jetson.GPIO for Jetson devices
Jetson.GPIO>=2.1.0; platform_machine=='aarch64'

