# Thư viện cần thiết cho OOBE BLE WiFi Setup
# ============================================
# Cài đặt bằng lệnh: pip3 install -r requirements.txt
#
# Tương thích:
# - Jetson Nano (JetPack 4.x)
# - Jetson Orin Nano (JetPack 5.x)
# - Jetson Xavier series

# ============================================
# HỆ THỐNG (Cài qua APT trước khi chạy pip)
# ============================================
# sudo apt-get install -y python3-dbus python3-gi bluez

# ============================================
# GPIO LIBRARY
# ============================================
# Jetson.GPIO cho Jetson devices
# Version >= 2.1.0 YÊU CẦU cho Jetson Orin Nano
# Version >= 2.0.0 cho Jetson Nano cũ
Jetson.GPIO>=2.1.0; platform_machine=='aarch64'

# Fallback cho development trên máy khác (không cần trên Jetson)
# RPi.GPIO>=0.7.0; platform_machine!='aarch64'

# ============================================
# PYTHON DEPENDENCIES
# ============================================
# Tất cả dependencies chính đã được cài qua apt:
# - dbus-python (python3-dbus)
# - PyGObject (python3-gi)
# - GLib bindings (python3-gi-cairo)

# Không cần thêm dependencies từ pip vì BlueZ stack
# sử dụng D-Bus và GLib đã được cài sẵn trong system
