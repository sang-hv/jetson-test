# Combined requirements (ai_core + logic_service + oobe/jetson_backend)
#
# Install:
#   python3 -m pip install -r src/requirements.txt
#
# Notes:
# - Keep exactly ONE of onnxruntime / onnxruntime-gpu depending on target.

# --- logic_service ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets
pyzmq>=25
aiosqlite>=0.20
pydantic>=2.0
cachetools>=5.3
boto3>=1.34
python-dotenv>=1.0

# --- ai_core ---
ultralytics>=8.3.0
insightface>=0.7.3
opencv-python>=4.8.0
onnxruntime>=1.16.0
# onnxruntime-gpu>=1.16.0
numpy==1.26.4
Pillow>=10.0.0

# --- oobe/jetson_backend ---
Jetson.GPIO>=2.1.0; platform_machine=='aarch64'

