# Face Recognition System - Dependencies
# Install with: pip install -r requirements.txt

# Core ML frameworks
ultralytics>=8.3.0          # YOLO v11 + ByteTrack (person detection + tracking)
insightface>=0.7.3          # Face detection + ArcFace embeddings

# Computer vision
opencv-python>=4.8.0        # Video capture and visualization

# ONNX Runtime (choose ONE based on your hardware)
onnxruntime>=1.16.0         # For CPU-only systems (including macOS)
# onnxruntime-gpu>=1.16.0   # For CUDA systems - uncomment and remove onnxruntime above

# Supporting libraries
numpy>=1.24.0               # Array operations
Pillow>=10.0.0              # Image loading for database

# Note: Models download automatically on first run:
# - YOLO: ~6MB (yolo11n.pt)
# - InsightFace: ~326MB (buffalo_l to ~/.insightface/models/)
