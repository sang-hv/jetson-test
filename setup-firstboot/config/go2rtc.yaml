streams:
  # go2rtc spawns start-stream.py directly, reads MPEG-TS from stdout.
  # Avoids TCP intermediary (fixes go2rtc panic on MPEG-TS SPS/PPS parse).
  camera:
    - exec:python3 /opt/stream/start-stream.py --mode exec

api:
  listen: ":1984"
  origin: "*"

log:
  level: info
